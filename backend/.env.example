# MongoDB Connection
# Get your MongoDB URI from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/classroom-booking?retryWrites=true&w=majority

# Email Configuration
# Option 1: Gmail (may not work on Render)
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASS=your_16_character_app_password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Option 2: Brevo (Recommended - Free and reliable)
# Get credentials from: https://app.brevo.com/settings/keys/smtp
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_brevo_smtp_key
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (Frontend URL)
# Replace with your actual Vercel deployment URL
FRONTEND_URL=https://your-vercel-app.vercel.app
